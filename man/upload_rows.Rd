% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/editUpload.R
\name{upload_rows}
\alias{upload_rows}
\title{Upload Edit Rows to CatMapper}
\usage{
upload_rows(
  df,
  database,
  form_data = list(),
  action = "add_node",
  add_options = list(district = FALSE, recordyear = FALSE),
  properties = NULL,
  merging_type = "0",
  api_key = NULL,
  poll_interval_seconds = 1,
  timeout_seconds = 600,
  url = NULL
)
}
\arguments{
\item{df}{Data frame or list of row objects to upload.}

\item{database}{Target database, typically \code{"SocioMap"} or \code{"ArchaMap"}.}

\item{form_data}{Named list matching CatMapper edit-page \code{formData}.}

\item{action}{Upload action option. Supported values:
\itemize{
\item \code{"add_node"}
\item \code{"add_uses"}
\item \code{"update_add"}
\item \code{"update_replace"}
}}

\item{add_options}{Named list with \code{district} and \code{recordyear} booleans.}

\item{properties}{Optional vector/list of upload property names to include.}

\item{merging_type}{Optional merging mode used by merge upload workflows.}

\item{api_key}{API key used for authenticated write actions. If \code{NULL},
\code{CATMAPR_API_KEY} is used.}

\item{poll_interval_seconds}{Polling interval in seconds while waiting for
queued upload tasks.}

\item{timeout_seconds}{Maximum seconds to wait for upload completion.}

\item{url}{API URL override. If \code{NULL}, \code{CATMAPR_API_URL} is used when set.}
}
\value{
A data frame built from the upload task result rows returned by the
API. The function always triggers a background \code{updateWaitingUSES} refresh
after upload completion; refresh trigger errors are suppressed.
}
\description{
Upload rows to CatMapper's \code{uploadInputNodes} endpoint using authenticated
write requests.
}
\examples{
\dontrun{
result <- upload_rows(
  df = data.frame(
    CMName = "Yoruba",
    Name = "Yoruba",
    CMID = "",
    Key = "Type == Adamana Brown",
    datasetID = "SD1",
    label = "ETHNICITY",
    stringsAsFactors = FALSE
  ),
  database = "SocioMap",
  form_data = list(
    domain = "ETHNICITY",
    subdomain = "ETHNICITY",
    datasetID = "SD1",
    cmNameColumn = "CMName",
    categoryNamesColumn = "Name",
    alternateCategoryNamesColumns = character(0),
    cmidColumn = "CMID",
    keyColumn = "Key"
  ),
  action = "add_uses",
  properties = c("variable"),
  api_key = Sys.getenv("CATMAPR_API_KEY")
)
head(result)
}
}
